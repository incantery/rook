# Moved

The Neovim navigator plugin now lives at
https://github.com/incantery/rook.nvim so it installs as a normal plugin
(`vim.pack.add({ "https://github.com/incantery/rook.nvim" })`).

`plugin/rook-navigator.lua` here is kept for reference and will be
removed; rook.nvim is the source of truth.
